# RESUME PLAN — Qubryx One (working dir /tmp/hrms_work, deliver to /mnt/user-data/outputs)
Last delivered: qubryx_fwn_v33.zip. Ritual: bump zip version, tsc --noEmit + npm run build before deliver, present_files on zip + FWN_Interaction_Map.md, append doc section. Stack React19/Vite/Tailwind v4/motion. Never ship broken build. Work in code (browser tools can't rebuild user's app).

## PHASE 0 + PHASE 2 (docs) COMPLETE (v36): entity backbone (types.ts Entity/Activity/Audience; store entities/activity + createEntity/updateEntity/publishEntity/archiveEntity/ackEntity; publish->employee cue materialization via Cue.entityId; ENTITIES seed in data.ts). UI: TeamDocs.tsx (manager/hr authoring, routed in Continuum documents region when lens!=employee), Documents.tsx 'Shared with you' + ackEntity, Q citation intent in answer() + QPanel 'ackEntity' action.
PHASE 2 COMPLETE (v37): manager Documents orbit node (Continuum ManagerHome); approvals-as-inbox (types LeaveToken status +'held'; store hold/resume/setDelegate/approveMany/rejectMany + approvalDelegate; ManagerApprovals rewritten as inbox with batch select, hold section, delegate banner).
PHASE 3 COMPLETE (v38): Tour.tsx persona-aware first-run tour (store showTour + setTour, localStorage 'qbx_onboarded_v1', replay in Overlays SettingsPanel, mounted in Continuum); new-joiner checklist (types OnbTask, data ONBOARD_TASKS/ONBOARD_BUDDY, store onboardingTasks/toggleOnboardingTask/onboardingBuddy, OnboardingCard in EmployeeToday rail); HR authoring (store candidates + assignBuddy, Onboarding component in Continuum); Q onboarding intent in answer().
PHASE 4 COMPLETE (v39): types Goal/OneOnOne/Review/ActionItem; data GOALS/ONE_ON_ONES/REVIEWS; store goals/oneOnOnes/reviews + createGoal/cascadeGoal/updateGoalProgress/archiveGoal/ooToggle/ooAddAction/submitSelfReview/saveManagerReview/nudgeReviews. Employee Growth.tsx extended (live goals +/- , next 1:1 agenda+actions, Q2 self-review submit). New TeamGrowth.tsx (manager/hr: Goals cascade+rollup / 1:1s / Reviews cycle) routed in Continuum growth region when lens!=employee; manager orbit 'reviews' node repointed to growth (label 'Growth'). Q intents in answer() for goals/1:1/self-review/review-cycle.
PHASE 5 COMPLETE (v40): Canvas v2 in VibeStudio (rename/duplicate/export-CSV/multi-turn-refine/drop-file; vibeApps gained title?/refine?; store vibeRename/vibeDuplicate/vibeRefine; localStorage persistence of vibeApps/vibePinned/vibeInsights/nowBoard via useEffect + lazy init). My World Board view: store homeView 'signal'|'board' (+setHomeView, persisted); Continuum HomeViewToggle + EmployeeBoard (Add-to-board palette myHours/myLeave/attendance/workload) gating EmployeeHome; NowBoard label extended.
v42 BOARD REDESIGN: nowBoard items scoped per-lens (lens:Persona field; boardAdd sets s.lens; NowBoard filters b.lens===w.lens; id `nb-<lens>-<kind>-<ref>`). Board is full-bleed: boardMode=(employee&home&homeView==board); rails (TheNow + Today) + orbit hidden in boardMode; faint dot-grid backdrop; boardArea full width. ModeSwitch = bottom-left floating pill (Signal/Board, employee home, both views) replacing old top HomeViewToggle. EmployeeBoard(area): top-left '+ Add card' popover (replaces center capsule); centered empty state; membership scoped b.lens==='employee'. Canvas send-to-Board id now `nb-<lens>-tool-<appId>`.
v41 FIXES: HomeViewToggle+EmployeeBoard palette repositioned below TopChrome (were hidden behind it, both top-3); toggle is employee/My World only. Canvas 'Send to…' menu (lens-aware Now label / Insights / Board) replacing 2 buttons; nowBoard gained type 'tool' (renders VibeApp on board, id nb-tool-<appId>). Refine now shows visible 'Refined · note' tag + clearer Q text.
DEFERRED: combine-tools-into-dashboard, PNG export, exact board position persistence.
v43 BOARD GRID FIX: employee board rebuilt as ordered CSS grid (BoardGrid in Continuum) — position=array order (persists), row-wise 4/col, drag+dragSnapToOrigin reorder via boardReorder(id,toIndex,lens) computing target from pointer over grid rect, vertical scroll on overflow. Manager/hr keep free-float NowBoard. Fixes position-reset-on-view-switch bug.
PHASE 6 PART 1 (v43): pulse surveys (state survey:PulseState [NOTE: named 'survey' not 'pulse' — collides with pulse() ripple method]; submitPulse; employee PulseCard in EmployeeToday; manager/hr Team-pulse summary [avg+sparkline+themes] in Analytics; Q intent 'pulse themes'). Agentic Q: 'Q recommends' card in Analytics (mgr) — flight-risk person → draft retention plan / prep 1:1 via w.ask.
v44 BOARDS-EVERYWHERE + RBAC: boardMode now any lens (w.region==home && homeView==board). BoardGrid + BoardChrome render for all lenses; BoardGrid filters b.lens===w.lens. EmployeeBoard renamed BoardChrome (lens-aware BOARD_PALETTE + BOARD_COPY: employee=myHours/myLeave; manager=attendance/workload/comp/retention; hr=attendance/comp/workload). ModeSwitch renders for all lenses. Removed free-float NowBoard render (fixes Canvas cards floating over Team/Org content) — NowBoard fn kept but unused. Q-to-board: answer() gained board?:{kind,ref}; Phase 6.2 intent (top of answer) maps metric words->refs, RBAC role-scoped, returns board+region home; ask reducer applies r.board (adds to nowBoard for current lens + homeView='board'). Analytics 'Send to Now'->'Add to board' copy. RBAC: Canvas already mgr/hr-only (ContextualAdd + VibeStudio gate); employees add via palette + Q only.
PHASE 6 PART 2 COMPLETE (v45): types CompRow/CompState/CoverageEntry + Region 'planning'; data COMP_ROWS (p1-p5 salary/min/mid/max), COMP_BUDGET 600000, COVERAGE (c1-c3). Store: comp:{budget,plan,status} + coverage:Record; actions compSet/compSubmit(budget+nonzero guard)/compReset/compApprove + setCoverage/autoCover. Planning.tsx (manager/hr, tabs Compensation|Coverage): budget meter (over blocks submit), per-person band bar + above/below-band flags (above=HR sign-off), Route-to-HR draft (status draft->submitted->approved; HR sees Approve); coverage list with covered-by select + gap warnings + Q-suggested + auto-assign. Continuum: region 'planning'->Planning (lens!=employee); manager orbit node 'Planning'; HR orbit node 'Planning'. Q intents: comp planning + coverage.
ROADMAP COMPLETE: Phases 0-6 all delivered & building clean.
OPTIONAL FEATURES DONE (v46):
- Publish-to-team: store sharedTools[] (persisted q_sharedtools) + publishTool/unpublishTool; Canvas Send-to menu 4th dest 'Team's/Everyone's My World'; employee BoardChrome palette 'Shared with you' section -> boardAdd('tool',appId) lens employee (BoardGrid renders since vibeApps is global).
- Employee bespoke cards: Viz MyGoalsViz (store goals owner ME_ID) + MyPayViz (data MY_PAY); Viz cases myGoals/myPay; BOARD_PALETTE.employee += myGoals/myPay; label() += My goals/My pay; Q-to-board map += goals->myGoals, payslip/my pay->myPay (employee role).
- Dashboard: vibeApps.parts?:string[]; action vibeCombine + method combineVibe; VibeApp template==='dashboard' renders parts stacked; VIBE_ICON.dashboard + vibeTitle('dashboard')='Dashboard'; VibeStudio select checkboxes + combine bar; parts passed to VibeApp at all 5 call sites.
- PNG export: html-to-image toPng on #cap-<appId> wrapper; PNG button next to CSV in Canvas. (Note: backdrop-blur may not capture perfectly.)
- Drag-position persistence: SUPERSEDED by ordered grid (position=order, already persisted).
CANVAS OVERHAUL PART 1 (v47):
- Store: canvasHistory[] (persist q_canvashist) + newCanvas/loadCanvas (New canvas saves current chat to history + clears; load restores). sharedTools gained to?:string (personId | 'team'); publishTool(appId,to?) + publishToolTo via same; employee 'Shared with you' filters to !to|'team'|ME_ID.
- VibeStudio LEFT: New canvas -> newCanvas(); 'Canvases' (current highlighted + canvasHistory -> loadCanvas); 'Artifacts' (vibeApps, click scrolls to #cap-<id>). RIGHT: 'Build a tool' (VIBE_TEMPLATES->createVibe) + 'Recipes' (CANVAS_MODULES now {id,label,desc,builds:string[]}; onClick builds each -> multi-tool). Combine checkbox moved to card TOP-RIGHT (absolute); removed from action row. Share-to-person submenu (sharePersonFor state) lists reports -> publishTool(appId, personId). PNG fix: exportPng adds body.png-capturing class (index.css neutralises glass -> solid #1a2230) + cacheBust + solid bg.
- BoardGrid: gridAutoRows CELL_H; dashboard tiles gridColumn span 2 + gridRow span min(3,1+parts) + internal panel-scroll; non-dash span 1x1 (fills row). Reorder math approximate with spans (acceptable).
v48 FIXES: (1) BoardGrid rebuilt as measured masonry — gridAutoRows 8px, rowGap 0, alignSelf start; per-card rowSpan = ceil((offsetHeight+16)/8) via useEffect+ResizeObserver (guarded setSpans); dashboards colSpan 2. Reorder via nearest-card-center hit-test (cardRefs). Removed forced min(3,1+parts) span + internal card scroll + layout prop. (2) VibeStudio left Artifacts = compact icon-chip wrap (click scrolls to #cap-<id>, dot if live) replacing full-width rows.
ITEM 6 PART 1 DONE (v49): 7 new Canvas tools — data (KPI_TILES, ATTRITION_TREND, HEADCOUNT_TEAMS, BAND_LADDER, ENGAGEMENT_HEAT, LEADERBOARD, HIRING_FUNNEL) + VibeApp render cases (kpi/attrition/headcount/bandladder/heatmap/leaderboard/funnel) + VIBE_ICON + VIBE_TEMPLATES entries + VibeStudio DATA_MAP for CSV. Icons added: BarChart3/Trophy/Filter/TrendingDown/LayoutGrid/Users. Canvas now 16 tools + 4 recipes. All support rename/dup/refine/combine/CSV/PNG/send-to/share.
ITEM 6 PART 2 DONE (v50): freeform widgets — vibeApps.config?:{kind,label}; store vibeWidget + createWidget + widgetKind + setWidgetKind; VibeApp WidgetView (kpi/gauge/trend/bars/table/timeline) with deterministic synth (wseed/wrng); config threaded to VibeApp at all call sites. vibeAsk extended: 7 new tool keyword maps (headcount/attrition/bandladder/heatmap/leaderboard/funnel/kpi-tiles) + freeform widget parse (kind regex + label extraction) + better fallback. VibeStudio: right-panel 'Widgets' section (WIDGET_KINDS primitives -> createWidget) + per-widget 'Shape' kind-switch chips. Q-to-board: board intent maps headcount/attrition/heatmap/leaderboard/funnel/bandladder (manager/hr); BoardGrid renders tool-template refs via VibeApp (else Viz).
NEXT: polish & fine-tune pass across all surfaces (user will drive). Canvas is feature-complete for now (16 tools + 6 freeform widgets + 4 recipes)., Phase 4 goals/1:1/reviews, Phase 5 Canvas v2 + World Board, Phase 6 surveys/comp/agentic.

## PHASE 1 COMPLETE + polish (delivered qubryx_fwn_v35.zip: voice hardened via src/useSpeech.ts used by TheCue + VibeStudio; login carousel redesigned).
NOTE: sandbox /tmp resets between sessions — restore with: mkdir -p /tmp/hrms_work && cd /tmp/hrms_work && unzip -oq /mnt/user-data/outputs/qubryx_fwn_vNN.zip && npm install. Next: Phase 0 entity-store backbone, then Phase 2 (manager doc CRUD + publish->employee cue + Q citations).

## DONE — Phase 1:
1. [x] Premium two-panel login (AuthGateway.tsx): large card, LEFT = feature carousel (in-product visuals, auto-advance, dots, reduced-motion aware), RIGHT = form (Qubryx One wordmark, email/pw, SSO, demo). Keep original orbital mark (QubryxOneMark) already in AuthGateway. Responsive: collapse left <880px.
2. [x] Grander Kudos: custom SVG "bloom" mark + richer burst + give-composer (pick teammate -> reason chips -> send). Receiving materializes a badge. Files: Synthesized.tsx (PulseLayer), EmployeeToday RecognitionCard, store KUDOS.
3. [x] Received-nudge cue: distinct cue kind 'nudge' with nudger avatar + one-tap resolve + soft ripple/sound. data.ts CUES (add employee nudge cue), TheNow.tsx KIND_META/act.
4. [x] Speech-to-text: Web Speech API on Cue (TheCue.tsx) + Canvas composer (VibeStudio.tsx) mic buttons; listening waveform state; graceful fallback.
Phase 0 backbone (start light): generic entity store pattern for later CRUD — defer unless quick.

## KEY FACTS
- store.tsx: useReducer; answer(text,state) intent brain incl typed actions (applyLeaveOn/clockIn/clockOut/nudge/approveOne/sim). Methods: applyLeave(k,mode,s,e,d), clockIn/Out, breakStart/End, approve/reject/bulkApprove, simulate(p,lever), boardAdd/boardRemove (nowBoard), pinVibe/unpinVibe/sendInsights/removeInsights, createVibe/vibeAsk, toast(msg,tone), pulse(x,y,tone), setWallpaper/setAvatarSeed/setMotionOff/setPlain. State incl soundOn, reduced, wallpaper, avatarSeed, nowBoard, vibeChat/vibeApps/vibePinned/vibeInsights.
- sfx (sound.ts): pickup/hover/merge/relate/ripple/confirm/reject/close/peel/droplet(soft bubble). haptics.
- Kudos data: KUDOS in data.ts. Recognition card in EmployeeToday.tsx (Heart burst on give).
- PulseLayer in Synthesized.tsx (flash+rings+particles).
- Cue bar: TheCue.tsx (bottom center, mic icon). Canvas composer mic in VibeStudio.tsx (attach() toast placeholder for image/file/mic).
- CUES persona-tagged in data.ts (kind union: wellness/sla/payroll/doc/risk/review/onboarding/headcount/coverage). Cue type has persona + kind + actionText.
- AuthGateway rendered OUTSIDE WorkspaceProvider (App.tsx) -> no useWorkspace there. Has QubryxOneMark + WallpaperArt(aurora) bg. Product name "Qubryx One". logo import '../assets/qub-logo.png'.
- brand vars: --color-lumen #52D0DD (teal/robin), --color-halo #6A3DFF (majorelle), --color-blue #3575C7. NO --color-robin.
- field-title/field-sub classes: light-mode high-contrast for on-background text.

## AFTER PHASE 1: package vNN, doc section, then Phase 2 (manager doc CRUD + publish->employee cue + Q citations) per dev plan (Phase0 entity store, Phase2 CRUD, Phase3 onboarding, Phase4 goals/1:1/reviews, Phase5 Canvas v2 + World Board view, Phase6 surveys/comp/agentic).
v51 VERIFIED-RESTORE CHECKPOINT: no code change. Restored qubryx_fwn_v50.zip into fresh /tmp/hrms_work, npm install clean (220 pkgs, 0 vuln), npx tsc --noEmit clean, npm run build exit 0 (only >500kB chunk advisory). Canvas unchanged: 16 tools + 6 freeform widgets + 4 recipes; lenses My World/My Team/The Org. Packaged as v51 to keep version chain + both docs in lockstep with the zip. FWN_Interaction_Map.md section 52 added. NEXT unchanged: user-driven polish & fine-tune pass across all surfaces.
v52 FOCUS + Q-BRAIN + UX PASS: (1) Region 'focus' (types.ts) + Focus.tsx (manager/hr): tasks derived live — mgr: pending leaves (SLA→critical) w/ approve/reject, flight risks w/ simulate, uncovered COVERAGE w/ autoCoverAll, not_started reviews w/ nudgeReviews, comp draft unallocated; hr: pooled payslips w/ overridePayroll, comp submitted w/ approveCompPlan, candidates>=90 no buddy, risks; + lens cues (sla/approval summary cue filtered out). PRIO colors coral/ember/halo/lumen + left rail; sort by rank; per-task Ask Q (w.ask), swipe-right clears (motion drag x>130, disabled when reduced), session done counter, skipped Set local. Entry: orbit node both homes (pulse when critical, count in value), TopChrome pill (mgr+hr, Crosshair), TheNow header Focus btn, 'f' key, Q intent. NOTE Focus not in PAGES → renders full-screen, rails collapsed. (2) qbrain.ts: LEXICON + fuzzyFix (restricted Damerau-Levenshtein, first-letter anchor, d<=1 len4-6 / <=2 len>=7, extraWords=people names) + understand(text,BrainState)->BrainResult|null; intents: greeting/help, focus/attention (guarded !comp|pay|salary|raise|merit), NAV table (verbs open/go to/take me to/jump to/navigate to — 'show' deliberately excluded to protect analytics queries; auto lens-switch w/ note), lens switch, chain approve-all+nudge (action kind 'chain' arg JSON steps), reject named (rejectOne), autoCover, nudgeReviews, goalCreate (title+dueOn parse), counts (pending approvals), who-is-on-leave, person lookup. Wired in store.answer(): fuzzyFix first, understand(state) second, legacy regexes third on FIXED string (so typos help legacy intents too). (3) QPanel onAction += rejectOne/autoCover/nudgeReviews/goalCreate/lens absent (unused)/chain (setTimeout i*450 recursive); input autofocus on qOpen. (4) Continuum: global keydown — '/'+Ctrl/Cmd-K focus #cue-input, Esc overlay→Q→vibe→home, 1/2/3 lens, 'b' homeView toggle (home only), 'f' focus (mgr/hr); one-time toast hint (localStorage q_kbhint); hooks placed BEFORE plain-mode early return. (5) TheCue: input id=cue-input; rotating lens-aware placeholder hints (4.2s interval). (6) data.ts SUGGESTION_GROUPS 'Team & insights' += focus/chain/auto-cover phrases. Caveats: brain is deterministic (no LLM runtime); swipe only clears, never commits. NEXT: continue polish pass user-driven; candidate follow-ups: employee-lens Focus, chain grammar beyond the approve+nudge pair, voice feedback on Focus actions.
v53 LOGIN + PALETTE-REMOVE + VIZ BRAIN: (1) AuthGateway: 4 Art SVGs rebuilt (shared ART_DEFS gradients lg-brand/lg-bar/lg-ember/lg-node/lg-halo; SMIL <animate> for waveform/typing dots/bar grow/pulses; orbit dual counter-rotating rings + initialled nodes; canvas scene shows prompt→bars+gauge+sparkline+'your data' chip; team scene presence dots + SLA pulse). Frame: gradient p-px ring wrapper + specular top line + tinted window dots + whileHover 1.015. Form: Mail/Lock leading icons, Eye/EyeOff toggle (showPw state; password default 'orbit-demo-2026'), focus ring shadow, keep-me-signed-in checkbox, KeyRound SSO, demo CTA card (Sparkles + copy), ShieldCheck SOC2 footer. Slide 3 copy ties to data-from-prompt. NOTE SMIL anims ignore reduced-motion (consistent w/ existing hubSpin). (2) BoardChrome: palette items toggle add/remove (find nb id by lens+ref+type, boardRemove + toast; hover swaps Check→X + 'remove'); shared rows = div w/ inner toggle button + dismiss X (opacity-0 group-hover) → store hideShared(appId): sharedHidden[] persisted q_sharedhidden, also strips tool board entries; filter list by !sharedHidden.includes. Recipient-side only — sharer Canvas untouched. (3) qbrain parseVizPrompt(text)->WidgetSpec[]|null: VIZ_KINDS synonym table (pie/donut→bars, percent/progress→gauge, roadmap→timeline etc, up to 3 kinds per prompt); extractVizData: A label-number pairs (VIZ_STOP filtered), B number runs (,|;|vs|and), C single pct→gauge val, D 'x vs y' labels (deStop each side), E comma word-list ≥3 →labels (articles stripped only — keeps 'build' as step); label: tail after for/of/about, cut at ':', strip kind words/digits, remove data-label residue else join labels ('Q1 · Q2 · Q3'). store vibeAsk: fuzzyFix first; hasInlineData(/\d/) skips template map so data wins; parseVizPrompt→new action 'vibeWidgets' (N apps template widget + user turn + per-app q turns); template fallback after; richer guidance msg. compperf regex tightened /comp\b|salary|underpaid/ + coverage lost bare 'cover' (protects 'compare'). vibeApps.config widened {kind,label,data?{labels?,values?}} (State+Action+VibeApp props). WidgetView: given values (or per-label synth) drive kpi (real delta from last two)/gauge (clamped %, % suffix)/trend (+x labels)/bars (values above bars + labels below)/table (label:value rows)/timeline (labels as steps); 'your data' badge when live; widgetKind reshape preserves data. VERIFIED: tsc+build clean; parse suite (bars pairs/trend run/gauge pct/multi-kind/vs labels/timeline steps/q1q2q3/12vs18/null for flightrisk). NEXT: polish continues user-driven; candidates: units in data ("₹", "h"), per-widget data editing UI, login art reduced-motion gate.
v54 LOGIN POLISH + FOCUS RAIL + CLEAN LANDINGS + STRICT LENS SCOPE: (1) AuthGateway: CTA shadow softened (0_8px_24px rgba(53,117,199,.55) + hover variant, no more halo bloom); carousel text block min-w-0 + max-w-[34ch] + break-words, h2 clamp(20px,2.2vw,26px), kicker truncate. Reduced-motion: module const MOTION (matchMedia), spin(dur,reverse) factory for ring <g> styles, <A> wrapper component emits <animate> only when MOTION (regex-swapped all 10 SMIL tags); product-mark hubSpin inline-gated. (2) TopChrome: Focus pills removed from manager+hr REGIONS, Crosshair import dropped. TheNow collapsed rail: was single button, now flex-col with Now toggle + Focus button (lens!=employee; coral ping when critical = mgr sla/risk or hr pooled; ring when region==focus). (3) Landing clean: initial panelLeft/panelRight=false; reducer 'lens' sets both false; 'region' home sets both false (else preserves); 'reset' → homeView signal + qOpen false + panels false + clears board/vibe. initial homeView hardcoded 'signal' (no longer restores q_homeview — clean launch guaranteed; setHomeView still persists but unused at init). (4) LENS SCOPE: data.ts SUGGESTION_GROUPS now Record<lens,groups[]> (employee: time/pay/docs+growth/organise; manager: desk/people/insights; hr: desk/org-insights/planning). TheCue + QPanel index SUGGESTION_GROUPS[w.lens]. store: answer() split → public answer() wraps answerRaw() with lens-scope guard: if r.lens && r.lens!==state.lens && !explicitSwitch(/switch|change|go...lens|view|world|team|org.../ or 'as an X') → return out-of-scope msg naming both lenses + key hint (1/2/3), NEVER switches. answerRaw = old body; fallback msg now lens-aware. qbrain: NAV foreign-lens → out-of-scope msg (no lens switch); chain gated lens!=employee (no forced manager); rejectNamed drops lens:'manager'; who-out + person-lookup gated lens!=employee (was leaking team data to employee); added employee 'what needs my attention' → personal to-do summary (region home). VERIFIED behaviorally: employee asking flight-risk/approve-all/comp/who-on-leave all refused w/ scope msg, in-scope employee (leave policy/hours) works, manager in-scope no-switch, explicit 'switch to my team' works. tsc+build clean. NEXT: polish continues; real deploys should hide top-bar lens tabs by held role; consider per-role availableLenses[] in state if a true employee-only build is needed.
v55 PREMIUM AUDIT PASS: index.html rebuilt (title 'Qubryx One — your day, in orbit', meta description, dual theme-color dark #0B0913/light #FBFAF7, color-scheme meta, inline SVG orbit favicon data-URI, fonts preconnect, viewport-fit=cover). index.css base layer additions: tap-highlight transparent; button:not(:disabled)+[role=button]+label:has(checkbox)+summary cursor:pointer (Tailwind v4 preflight regression), disabled not-allowed; global press feedback button:active scale(.97) with .no-press opt-out (motion inline transforms override safely); ::selection lumen 30%; input/textarea caret-color lumen + placeholder trace; h1-h3 text-wrap balance; img user-select/drag none. @media (hover:none): .hover-reveal opacity .8 (applied to BoardChrome shared dismiss X), .corner-tab .9. TopChrome header max-w-[96vw] + island overflow-x-auto hide-scrollbar. Continuum stage motion.div key now `${nonce}-${lens}-${region}` (was nonce) — every nav replays spring enter (opacity+scale .97+y6, stiffness 160 damping 24; reduced=fade); VERIFIED TheCue/QPanel/TopChrome/TheNow/Toasts outside keyed wrapper (lines 175+) so no input/chat state loss; region-local state (Focus skipped set) resets on nav = acceptable. Audit verdict logged in map §56 incl. already-strong list + future recs (aria-label sweep ~12 real gaps, scroll-margin anchors, vibrate() haptics, role-gated lens tabs). tsc+build clean. NEXT: user-driven; screenshots would calibrate visuals further.
v56 SCREENSHOT ROUND — GRAPH SCALE + SEMANTIC COLOR + LOGIN TUNE: (1) layout.ts rebuilt: multi-ring seeding (up to 5 rings, cap = floor(RamanujanCirc(a,b)/(TW*1.02)), min 4/ring, alt rings offset PI/n, r+=132, a/b clamped maxRx=w/2-TW/2-20 maxRy=h/2-92, SQUASH .78) THEN deterministic relaxation (40 iters, CW=218 CH=72 boxes, push apart on least-overlap axis, centre frozen [pushes others 2x], clamp to area, early-exit) → links from FINAL positions. Stress-verified 0 overlaps/0 OOB at n=5/12/16/24/30, viewports 1100x620→1870x760. Auto-roster >8 unchanged. (2) SEMANTIC COLOR LANGUAGE (single source, = Focus PRIO): coral=act-now/at-risk, ember=due-soon, halo=people/insight domain, lumen=operational domain/healthy. Rules: accents STATE-DRIVEN (relax to lumen when clear); coral+pulse EXCLUSIVE to Focus sphere (domains cap at ember). Applied: TheNow CUE_TINT remapped {risk:coral, sla/coverage/payroll:ember, doc/review/onboarding/headcount:halo, wellness/nudge:lumen}; Continuum home nodes — employee leave/pay→lumen, documents→ember-when-pending; manager approvals→ember-when-pending+'clear' value, team/growth/documents→halo, availability→lumen, planning→ember when planningDue (uncovered<3 or comp draft empty); hr org→halo, payroll→ember-when-pooled+'flowing' value, onboarding→ember when buddyDue, policies coral→ember, planning→ember when comp submitted; Tokens PersonTokenView risk ember→coral (tint/border/dot/glow); constellation risk links ember→coral. (3) AuthGateway: subtitle 'Sign in — everything picks up where you left off.' (orphan fix); art height 172→196, art-block mb 7→6 (dead-space). tsc+build clean. NEXT: user-driven; candidates: graph zoom/pan for 40+, dept clustering rings.
v57 GALAXY ORG + COLOR TRUTH + PAY MASK + LOGIN CHIP: (1) data.ts hierarchy: m1→[p1 Alex,p2 Sarah,p3 David,p4 Elena,p5 Priya,p10 Aisha]; p2→[p6,p7,p11,p13,p15,p17] eng pod; p3→[p8,p12,p16,p18] design pod; p10→[p9,p14] data. team node value dynamic `${reports.length} reports`; answerRaw team reply de-hardcoded. layout.ts: relax() factored standalone (160 iters, frozen set, clamp); computeOrgTree(people,area): kidsOf map, roots=no manager in set (multi-root galaxy), attempt(stackH null/3/4) escalation until totalW<=availW (leaf stack cols TW=210 gap16, branch blocks gap40, LEVEL_GAP 118, STACK_GAP 84), place() root-centered, linkSpecs→cubic bezier d strings from FINAL positions, overflow→relax→if still bad flow-grid rescue (never-overlap guarantee). Link type += d?: string. Continuum: hr lens→computeOrgTree(visible), links render <path d> when present else <line>. VERIFIED 0 overlap/0 oob at 1870/1300/1100/900 widths; mgr radial = 7 cards (directs) fine. (2) COLOR: CUE_TINT doc+nudge→ember (asks); employee Pay sphere payDue=cues payroll → ember (mirrors cue, relaxes together); Roster STATUS flight_risk ember→coral, on_leave coral→halo, card borderLeft coral. (3) PAY MASK: store showPay (persist q_showpay) + togglePay; Pay sphere value showPay?'₹1.45L':'₹ ••••', hint mentions eye; PayDeep component (masked stat rows for numeric values + Reveal/Hide amounts pill, Eye/EyeOff, stopPropagation) replaces NODE_DEEP.pay usage; RiverOfCash amounts wrapped in <Masked>; PayslipTokenView already Masked. (4) AuthGateway confirm chip rect x130 w150 + text centre-anchored x205 fs11; canvas caption fs11. tsc+build clean. NEXT candidates: manager subtree tree view, skip-level Focus policy, auto-re-mask timer, dept cluster hulls on tree.
v58 THE ORG ROUND — LINEAGE + PAY RUN CENTER + REGION ADDS + TIME ADMIN: (1) LINEAGE: layout Link += fromId/toId (computeOrgTree link() passes linkSpec ids); Continuum orgHover state + lineage useMemo (walk managerId, guard 12); person cards wrapped in div onPointerOver/Out (hr only); dimmed = lineage ? !in-lineage : dimPerson; links lit when both ends in lineage (lumen .95 w2.2 drop-shadow, others fade to .12 during hover). (2) RiverOfCash.tsx REWRITTEN → Pay Run Command Center: stage rail (stage = anomalies?1:2 of Calculated/Anomaly review/Approval/Release), totals chip (Masked via w.showPay + eye toggle), pooled anomaly cards (overridePayroll + w.ask), register (deterministic netFor(name, role) seeded, per-person ready/pooled, masked), exportCsv via Blob download (amounts 'MASKED' unless showPay — confidentiality preserved in export), approve-run blocked while anomalies, history chips from released payslips. Stale 'river' copy swept (sphere hint, suggestions, fallback). (3) ContextualAdd region-aware (hr): people/home→addPerson overlay; onboarding→addCandidate (prompt); payroll→off-cycle + export toasts; timeadmin→addType; other→'Leave types & holidays' shortcut; manager LOST 'New leave type' (HR-only). Overlay type += 'addPerson'; Overlays.tsx AddPerson modal (name/role/dept select/reports-to = leads only) → store addPerson (appears in tree+roster live). (4) TIMEADMIN: Region += 'timeadmin'; Holiday type {id,label,date,kind national|company|restricted}; data INITIAL_HOLIDAYS (6); store holidays + addHoliday/removeHoliday/setAllot(+leaveTypes balance)/addCandidate; TimeOffAdmin.tsx (leave types w/ ± steppers, new-type form via existing addType, holiday list grouped/badged w/ hover-reveal remove, add form w/ kind segmented, announce toast); HrHome sphere 'Time & holidays' (halo), TopChrome hr pill 'Time', qbrain NAV /holiday|leave types|allotment/→timeadmin (hr). AUDIT + recs in map §59 (reconciliation view, per-joiner checklists, ack nudge-all, calendar single-source refactor — admin holidays don't render on employee calendar yet, exit region). tsc+build clean. NEXT: user-driven.
v59 PHASE A — EXPLAINABILITY: QMsg += rationale?{why, whyNow?, whyNot?, confidence?'high'|'medium'|'low', evidence?[]} (optional → no regressions). New RationaleDisclosure.tsx (reusable; 'Why this?' chevron, AnimatePresence height expand, confidence dot lumen/ember/coral, evidence bullets, honesty footer; indent prop for Focus). Rendered in QPanel (after text, before source/action) + Focus cards (indent=false, after Ask Q). Attached rationale on: qbrain person-lookup flight-risk (conf from attendance band <0.78 high / <0.85 med, evidence real att/vel), qbrain autoCover (medium, match logic), Focus manager flight-risk task (conf attendance<0.78), store answerRaw retention reply (high, 0.78/↓22%/backfill), store payroll reply (high, safeguard/tax-trigger). NOT on nav/other (verified). Confidence traces to real signal (rule); whyNot states the limit; footer 'not a guarantee'. VERIFIED tsx: risk=high/3-evidence, payroll=high, coverage=medium, nav=none. tsc+build clean. NEXT: Phase B discoverability. (Later: extend rationale to comp/review/onboarding intents.)
v60 PHASE B — DISCOVERABILITY: (1) telemetry.ts: emit(interaction, surface) at store dispatch choke-point (wrapped d = useCallback emitting `${lens}:${region}` via surfaceRef then dRaw; imports useCallback/useRef); buffer[500] in-mem, dev console mirror '[telemetry:not-connected]', connectTelemetry(sink) one-line hook (NOT connected — no backend, honest), GRAMMAR_INTERACTIONS set (peel/combine/stack/boardAdd/boardRemove/vibeCreate/vibeWidgets/publishTool/toggleSelect/setHomeView/explainToken). VERIFIED emits+sink+tagging. (2) GestureLegend.tsx: on-demand cheat-sheet (9 gestures def+how + key map), reached via '?' key (Continuum keydown), 'Gestures' pill in TopChrome (HelpCircle, setLegend), tour final step 'See all gestures'→finish+setLegend. store: showLegend state + 'legend' action + setLegend. (3) Contextual one-time hints (Continuum useEffect on region/homeView): q_hint_board, q_hint_focus (localStorage-keyed, toast); first-run q_kbhint updated to mention '?'. (4) Empty Board state: added 'Build one in Canvas' (setVibe) + 'See the gestures' (setLegend) buttons (was pointer-events-none only). (5) Verb audit: grammar consistent (Ask Q/Stack/Combine/Send to uniform; 'Add to board' vs 'Send to' intentionally distinct) — no renames. Wand2 import added to Continuum. tsc+build clean. NEXT: Phase C universal search.
v61 PHASE C — UNIVERSAL SEARCH: qbrain BrainState += documents?/holidays? (optional; store State already has them). New searchEntities(s, state) before understand() intents; called from a gate in understand step 0.5: fires only when !hasVerb (approve/apply/open/show/... list) && !isQuestion (how/why/what/who is/...) && wordcount<=4 && len>=2 — so verbs/questions never hijacked (VERIFIED). Coverage: people (role/dept/reporting-chain-to-root via managerId walk guard12/directs/att/vel/status/pending-leave + context action: hr→org graph, mgr+risk→sim, mgr→team), documents (title substring, version/category/ack status, region+source), leaveTypes (label/id match, balance, calendar|timeadmin action), holidays (label/date match, kind, calendar|timeadmin action). LENS SCOPE: employee people-search allowed only for self(p1) or own manager(me.managerId) else polite refusal; non-people entities open to all; mgr reports+leads, hr all. Discoverability: TheCue placeholders teach 'Search: type a name...'; employee fallback msg mentions search. VERIFIED tsx: hr/mgr/employee scoping, handbook/earned/Diwali resolve, approve/why/show-comp/book-leave NOT hijacked. tsc+build clean. NEXT: Phase D analytics/timeline (2 rounds). Caveat: modelled domains only (no full-text index); substring match.
v62 PHASE D — ANALYTICS/TIMELINE (both rounds): (1) data.ts METRIC_SNAPSHOTS: MetricSeries[] {key,label,unit,goodDown,periods[],values[],scope org|team}; 7 metrics — attrition/headcount/tenure/engagement (org, PERIODS_MONTH Dec→Jul), attendance/pending/risk (team, PERIODS_WEEK). MetricKey type exported. (2) analytics.ts (framework-free): computeDelta(key, back=1) → {from,to,labels,abs,pct,improved(goodDown-aware),flat}; narrate(d) plain-language w/ pts/y/plain units + improving|worth watching; whatChanged(scope,back) sorted by |pct| excl flat; fmtValue. (3) qbrain change-detection block (step 1.7, lens!=employee, before nav): wantsChange/wantsTrend regex + metricMap 7 + back=3 if quarter/q1/since-dec else 1; 'what changed' (no metric) → top-3 whatChanged bullets + rationale(evidence=readings); named metric → narrate + region analytics + board {chart ref attrition/attendance/engagement/headcount} + rationale(conf from flat). Imports anComputeDelta/anNarrate/anWhatChanged/anFmt. VERIFIED lens-scoping + good/bad framing + span + no hijack. (4) WidgetView trend: before→after line (first→last + ▲/▼ %over-period badge lumen/coral) when live & >1 pt. (5) store vibeAsk: metric-trend path before template map — /trend|over time|.../ + !digits + metricMap → vibeWidgets trend seeded w/ METRIC_SNAPSHOTS series (real labels+values). Imports METRIC_SNAPSHOTS, MetricKey. (6) SUGGESTION_GROUPS manager/hr insights += trend prompts. tsc+build clean; inline viz path intact. NEXT: Phase E memory (demo-grade localStorage, honestly labelled). Caveat: snapshots seeded (not warehouse-live); 'why it moved' not claimed.
v63 PHASE E — MEMORY (demo-grade, device-local): memory.ts — MemoryThread{id,lens,topic,entity?,lastText,at,count}; localStorage 'q_memory_threads_v1' max12. extractTopic: skips len<6, greetings/show/open/go, pulls proper-name or topic-keyword or first-6-word clause. remember(lens,text) dedup-bumps same topic. lastThread(lens, olderThan=60s) → most recent PRIOR-session thread (guard against mid-convo echo). threadsFor(entity,lens). clearMemory/allThreads. Store: import remember/lastThread/MemoryThread; remember(s.lens,t) hooked into ctx ask + runIntent; recallThread():lastThread(s.lens) on ctx. QPanel: recall = qOpen&&qLog empty ? recallThread() : null; recall chip above suggestions (History icon, re-asks entity?`Tell me about X`:lastText). qbrain searchEntities person path: memThreadsFor(firstName,lens) filtered >60s → priorNote appended (Phase C tie-in). Overlays SettingsPanel: 'Clear Q's memory' button (clearMemory, subtext 'this device only'). VERIFIED tsx: extraction skips greetings/nav, store+dedup(count bumps), 60s guard, entity lookup, clear. tsc+build clean. === PHASES A–E ALL COMPLETE (v59–v63) ===. Honesty boundary: telemetry wired-inert (v60), memory device-local (v63) — both 1 integration from production. NEXT: user-driven; earlier logged candidates remain (calendar single-source, payroll reconciliation, per-joiner checklists, offboarding, prod RBAC lens-gating).
v64 BACKLOG-1 CALENDAR SINGLE-SOURCE: data.ts holidaysToEvents(holidays[]) — parse "D Mon" vs MONTHS, national/company→kind 'holiday', restricted→'rh' + "Optional: X (RH)" label, skip out-of-window (Jun–Sep). Calendar.tsx: useWorkspace().holidays; events = useMemo([...CALENDAR.filter(kind!=holiday&&!=rh), ...holidaysToEvents(holidays)]); local eventsOn from events; monthLeaves from events; DetailGrid gets eventsOn prop (was module-level CALENDAR). qbrain: next-holiday intent (employee) reads state.holidays → first + count + restricted count, region calendar. VERIFIED tsx: derivation company/national→holiday, restricted→rh, HR-add Sep appears, OOB skipped. tsc+build clean. NEXT backlog: payroll reconciliation (run-over-run deltas). Caveat: WEEK_AHEAD rail still own seed (cosmetic); window Jun–Sep only.
v65 BACKLOG-2 PAYROLL RECONCILIATION: RiverOfCash — prevNetFor(id, curNet): RAISE{p3,p7,p12}→/1.08, BONUS_LAST{p5,p16}→+25k, NEW_JOINER{p9}→null, LWP_REVERSED{p11}→-12k, else same; reasonFor(id) strings. rows now carry prev/delta/reason. Tabbed register|reconcile (useState tab); Reconcile: roll-up Jun→Jul total + runDelta chip (ember up/lumen down) + changed/steady counts; changed rows (reason + before→after + signed delta, maskDelta respects showPay); new-joiner rows tinted separate; footer 'every delta traces to a reason' + Ask Q. Imports GitCompare, ArrowRight, useState. store: recon intent (/reconcil|payroll chang|why did payroll|run-over-run.../) before pooling intent → region payroll + rationale (evidence grouped counts). ContextualAdd payroll: 'Reconcile vs last run' → ask. HR suggestions += 'Why did payroll change this month?'. VERIFIED tsx recon intent+rationale, plain payroll unaffected. tsc+build clean. NEXT backlog: per-joiner onboarding checklists. Caveat: prev run reconstructed (not stored); reason inference rule-based.
v66 BACKLOG-3 PER-JOINER ONBOARDING CHECKLISTS: types OnbChecklistItem{id,label,owner,done} + OnboardingCandidate += role?/startDate?/checklist?. data.ts ONBOARD_TEMPLATE (8 items w/ owner) + makeChecklist(prefix, doneCount); ONBOARDING seeded w/ role/startDate/checklist (o1=2 done, o2=4, o3=7); imports OnbChecklistItem. store: toggleJoinerTask action (flip item + recompute progress from checklist %); assignBuddy also ticks /buddy/ item + recomputes; addCandidate seeds makeChecklist(0) + progress 0; ctx toggleJoinerTask; data import += makeChecklist (line 2 fixed 'VIBE_TEMPLATES , MetricKey' spacing). Continuum Onboarding() REBUILT: per-joiner card (Avatar, role·starts, New tag, derived progress done/total, tappable checklist items w/ strike + owner tint {IT lumen, People halo, Manager ember, Joiner mist}, buddy select). HR onboarding sphere value=candidates.length, deep=onbDeep (count/closest/awaiting-buddy dynamic); qbrain-in-store onboarding intent narrates live counts + closest + noBuddy. VERIFIED tsx: 8-item template, seed 2/4/7, toggle recompute 33→38%, owners present. tsc+build clean. NEXT backlog: policies 'nudge all non-acknowledgers' bulk action; then offboarding region; then prod RBAC lens-gating. Caveats: one template for all (not role-varied); manual tick (no MDM/e-sign signals); employee-side first-week list still separate seed.
v67 BACKLOG-4 POLICIES NUDGE-ALL: store entityNudge action + nudgeNonAckers ctx: expected = org?people.length : team?reports-of-target : 1; pending = expected - ackedBy.length; re-surfaces ack cue if viewer is a pending target; logs activity + toast; pending=0 → 'everyone acknowledged' info. TeamDocs: per published+requiresAck card computes expected/pending; shows 'N of M acknowledged'; ember 'Nudge N pending' button (Bell icon) when pending>0. QPanel onAction 'nudgeAll' → nudge every published ack-required entity + setRegion documents. store answerRaw HR nudge intent (lens!=employee, /nudge|remind|chase.../) → summary + action nudgeAll. ContextualAdd hr documents: 'Nudge non-acknowledgers' → ask. HR suggestions += 'Remind those who haven't acknowledged'. VERIFIED tsx: 8 pending (10 org-2 acked), nudgeAll action, employee gated. tsc+build clean. NEXT backlog: offboarding/exit region; then prod RBAC lens-gating. Caveats: audience derived not real distlist; nudge = in-app cue+log not email/slack; 'nudge everyone' phrasing trips scope-guard (other phrasings+button fine).
v68 BACKLOG-5 OFFBOARDING/EXIT REGION: types Leaver{id,name,role,lastDay,reason(Resigned|Contract end|Retirement|Involuntary),progress,checklist}. data OFFBOARD_TEMPLATE (8 owner-tagged, ordered: ack→knowledge→reassign→assets→exit interview→settlement→ACCESS REVOKE→alumni) + makeExitChecklist + OFFBOARDING seed (x1 Tomas resigned 2/8, x2 Chloe contract-end 5/8); Leaver imported. store: leavers state (OFFBOARDING), toggleExitTask (flip+recompute), startOffboarding(personId,reason,lastDay) (dedup by name, seeds makeExitChecklist(0), activity+toast); ctx both. Offboarding.tsx: per-leaver card (avatar, last day, reason badge tint, derived progress, checklist w/ owner tints, ACCESS-REVOKE locked until knowledge+asset done w/ warn toast + AlertTriangle), 'Start offboarding' inline form (person/reason/lastDay), empty state. Continuum: import + region render (hr+exit) + HR sphere 'Offboarding' (ember when leavers, exitDeep count/next/avg). TopChrome hr pill 'Exits' (UserMinus). qbrain NAV exit entry (before onboard). store answerRaw exit intent (MOVED before /(team|report|who)/ catch-all; earlier dup deleted). ContextualAdd hr exit region option. HR suggestion 'Who is offboarding?'. NLU FIXES: fuzzyFix lexicon += offboarding/leaver/resignation (was correcting offboarding→onboarding); HR onboard intent /(?<!off)(onboard|new hire|joiner)/ + !offboard guard; exit intent before team/who catch-all. VERIFIED tsx: who-is-offboarding→exit, open-offboarding→exit, onboarding-status→onboarding independent, team-q still scope-guards. tsc+build clean. NEXT backlog (last): prod RBAC lens-gating by held role. Caveats: access-lock is demo guard not IAM; startOffboarding doesn't remove from graph (stays till last day, no scheduled transition); fixed reason set.
v69 BACKLOG-6 RBAC LENS-GATING (backlog complete): State += role: Persona + availableLenses: Persona[]; lensesFor(role) helper (employee→[employee], manager→[employee,manager], hr→all); initial role from localStorage q_role default 'hr' (demo unchanged). Action 'role' (setRole, persists, remaps availableLenses, clamps current lens); 'lens' action now refuses unavailable (warn toast). ctx setRole. TopChrome: LENSES.filter(availableLenses). Keyboard 1/2/3 safe via action gate. answer() scope-guard RBAC-aware (canAccess ? 'switch to X' : 'your role doesn't have access'). Overlays Settings: 'Role & access' section (3 role buttons w/ hints, persists) + destructure role/setRole. VERIFIED tsx: manager→HR query = no-access msg, HR explicit switch works, lensesFor hoisted. tsc+build clean. === BACKLOG COMPLETE (v64–v69) + REVIEW PHASES A–E (v59–v63) ===. Deferred = real-deployment only: telemetry sink, cross-device memory, search index, metric warehouse, IAM/e-sign, server-enforced RBAC. Caveats: gates lenses not per-region/action; role is device-local demo (real RBAC server-enforced, client gating is UX only).
v70 VISUAL PARADIGM (peel / closeable orbit / icon language): (1) PEEL real anim Layer-2 only — Tokens.tsx TokenFrame: when p.deep, wrap face+deep in relative div; isPeeled → deep layer renders in flow (establishes height) + peeling-face absolute overlay lifts from origin-top-right (rotateX -38 rotateZ 7 y-12 x8 + boxShadow, 0.34s) via AnimatePresence; reduced → crossfade. Layer 1 expand unchanged accordion. (2) CLOSEABLE ORBIT — OrbitNode += concept?; HomeOrbit filters w.hidden before layout; sphere render is hero (ConceptIcon size hero on top, label+value beneath) + onClose={()=>w.hide(key)} (TokenFrame already had bottom-right X). Persist: store SPHERE_KEYS set; init hidden from localStorage q_closed_spheres; useEffect persists hidden.filter(SPHERE_KEYS) only (transient token hides session-only). ContextualAdd: 'Closed cards' label, SPHERE_LABEL+SPHERE_CONCEPT maps, restore rows show ConceptIcon. (3) ICON REGISTRY src/icons.tsx — CONCEPT{46}: {icon,tint}; ICON_SIZE hero30/card20/inline14; ConceptIcon({concept,size,tintOverride,strokeWidth}) binds glyph→TINT_VAR. Applied: Continuum orbit (hero, 23 nodes got concept), ContextualAdd restore (inline), Calendar leave-type buttons (card), TimeOffAdmin rows (card). tsc+build clean. Caveats: un-peel crossfades (no reverse peel); person/payslip keep avatars; remaining secondary lists incremental.
v71 ORBIT SPHERE LAYOUT REVERT: v70 hero stack looked crowded (screenshot). Continuum HomeOrbit sphere face → back to horizontal (flex items-center gap-3): icon chip left (w-11 h-11, was w-8 h-8 original / was w-14 hero), ConceptIcon size 'card' (20px), pulse dot on chip; label+value stacked right. Sphere y offset back to p.y-30 (was -34). All other v70 work intact. tsc+build clean.
v72 PEEL-TOGGLE / DOC-VIEWER / CALENDAR: (1) Tokens.tsx peel button toggles w.peel(isPeeled?null:id), visible while peeled, struck ScanEye (diagonal line span) when active. (2) DocumentViewer.tsx overlay (docView state + openDoc action in store; mounted in Continuum next to Overlays); synthBody() fabricates policy body from summary; Documents.tsx cards: leading category icon (docConcept), Read btn→openDoc. icons.tsx += doc.Handbook/Policy/Benefits/Compliance/announcement/note/goal + docConcept(). (3) Calendar.tsx: data.ts dayStatus(m,d) present/absent/leave/weekend/none (SEEDED_LEAVE 1:14,1:15,0:24; SEEDED_ABSENT 1:8,0:16; seeded>weekend; future=none). Detail grid cell status dot + ribbon dot att ring + legends. Layout: two-col grid lg:grid-cols-[1fr_360px] — left scroll (calendar+context), right sticky composer (was single clipped scroll). Composer reworked vertical: prominent balance card (bal/after/over), type grid, duration modes + half-day 1st(AM)/2nd(PM) toggle (half state), dates, summary, send. tsc+build clean. Caveats: attendance+doc body seeded (→ service reads in enterprise); un-peel crossfades.
v73 WORKSPACE Phase 1-2: third home view 'workspace' (all personas). types.ts: WsKind note|list|todo, WorkspaceItem{id,lens,owner,kind,title,color,pos,size?,z, noteHtml?, listStyle?,listItems?[WsListItem{text,depth}], todos?[WsTodoEntry{text,done,deadline?,timerMs?,subtasks[WsSubtask],ordered?}], flowboardId?,sharedWith?,sharedBy?}, WsConnector. store.tsx: homeView+='workspace'; state wsItems/wsConnectors/wsZ persisted q_wsitems/q_wsconn; actions wsAdd(kind,x,y default colors note#FFB454/list#52D0DD/todo#6A3DFF)/wsUpdate(id,patch)/wsMove/wsDelete(also drops connectors)/wsFront(z++); ctx wsAdd/wsUpdate/wsMove/wsDelete/wsFront. Continuum: import Workspace + PenSquare; workspaceMode=region home&&homeView workspace; render <Workspace area={boardArea}/>; ModeSwitch 3 btns; b-key cycles signal→board→workspace. Components/Workspace.tsx: dot-grid surface, GRID=24 snap, +Add menu (note/list/todo), WsCard (drag dragMomentum=false, snap on dragEnd, wsFront on pointerdown, header: grip/title-rename/color-picker 8 swatches+custom/delete). NoteBody: contentEditable + execCommand toolbar (bold/italic/hiliteColor/ul/ol/fontSize/foreColor) + 5 fonts (ws-font-inter/worksans/lora/playfair/mono in index.css + google import). ListBody: bulleted/numbered, depth nesting Tab/Shift-Tab (max 4), markers •◦▪ / 1.a., Enter add/Backspace remove. TodoBody/TodoEntry: done/deprioritize(send last)/delete/edit/add, deadline date + timerMs(min), subtasks bulleted|numbered(ordered) individually checkable, per-entry progress bar from subtask %. index.css += ws fonts + .ws-note-body list/placeholder styles. tsc+build clean. NEXT: Phase 3 flowboards/connectors, 4 Q+app integration, 5 signal/panel per-item, 6 sharing + Canvas synergy.
v74 WORKSPACE Phase 3 FLOWBOARDS: store.tsx actions wsConnect(fromId,toId — unify flowboardId, reuse existing id from either end else fb${Date.now()}, merge across boards; connector {id,fromId,toId,color=from.color})/wsDisconnect(id — remove conn + recomputeFlowboards)/wsConnUpdate(id,patch label/color); wsDelete now recomputeFlowboards. recomputeFlowboards(items,conns): connected-components (DFS over adjacency), fbId=`fb-${min member}`, unconnected items lose flowboardId. ctx wsConnect/wsDisconnect/wsConnUpdate. Workspace.tsx: rects ref (surface-relative x,y,w,h per card via ResizeObserver+item.pos), connectFrom/ptr/selConn state. Connect handle (Link2, right edge, hover) onPointerDown→setConnectFrom; surface onPointerMove tracks ptr, onPointerUp hit-tests rects→wsConnect. SVG layer z1 below cards: bezier(a,b) with edgePoint() border intersection, transparent 14px hit-path + visible stroke + target dot + mid label; live dashed drag line. Connector inspector popover (label input/4 colors/remove X) at mid-point. WsCard: drag={!connectFrom}, reports rect, connect handle, inFlowboard badge (Workflow icon) + tinted ring. Persist q_wsconn + flowboardId in q_wsitems. tsc+build clean. NEXT Phase 4: single item→Signal/right-panel + drag-to-Q for action items (leave/calendar/docs); whole-flowboard NOT sendable. Phase 5 sharing. Phase 6 Canvas synergy.
v75 WORKSPACE Phase 4 (item→Q/Signal): store.tsx helpers wsItemText(item) (note: strip <>, list/todo: join entries+subtasks) + wsActionItems(item) keyword scanner → [{text,action?}] (leave→nav calendar, meet/1:1/review→growth, policy/doc/sign→documents, pay/comp→payroll, deadline→todo hint). Actions wsToQ(id): build QMsg from summary+action bullets, first action bound, rationale, push to qLog + qOpen. wsToSignal(id): if flowboardId→toast refuse; else nowBoard += {type:'ws',ref:id}, homeView='signal'. nowBoard type union += 'ws'. ctx wsToQ/wsToSignal. Workspace.tsx WsCard: ⋯ menu (MoreHorizontal) state `menu`: "Ask Q for actions"→wsToQ, "Send to Signal"→wsToSignal (disabled if inFlowboard + hint). Continuum.tsx: WsSignalPins({area}) renders nowBoard type=='ws' items top-right of signal home (title, kind preview, Ask Q, open-in-workspace via setHomeView('workspace'), unpin boardRemove); mounted in signal home block. BoardGrid+NowBoard nowBoard filters += b.type!=='ws'. tsc+build clean. NEXT Phase 5 sharing (item.sharedWith→recipient workspace, reuse sharedTools pattern). Phase 6 Team/Org Canvas synergy (item→Canvas tool, Canvas→workspace ref card).
v76 WORKSPACE REFINEMENT: types.ts WsConnector += fromSide/toSide/arrowStart/arrowEnd. store wsConnect(+sides, default arrowEnd:true) + wsResize(id,w,h); ctx updated. Workspace.tsx major rewrite: (1) infinite canvas — pan state {x,y}, panning ref, drag empty/[data-canvas] to pan, world layer translated by pan, dot grid via backgroundPosition, Recenter btn, screenToWorld(). (2) WsCard corner grammar like tokens: TL move (useDragControls, handle-only drag), TR close(X), BR resize (Maximize2, pointermove resize min200x120, snap on up), BL send/share menu (Send→wsToQ/wsToSignal). Header slimmed to ReorderHeader (icon+title+color). Card height=size.h, body flex-1 overflow-auto. (3) 4-sided anchors SIDES top/right/bottom/left, sidePos map, onStartConnect(side); surface onPointerMove tracks connect+hoverTarget (bestSide facing), onPointerUp→wsConnect(id,target,fromSide,toSide). SVG in world layer translate(6000,6000) 12000px; pathFor() perpendicular bezier via extend(); per-connector markers ah-{id}/ah-s-{id}. Inspector: label/5 colors/arrowStart/arrowEnd toggles/remove. Snap guide dashed lines while dragging (onSetSnap). (4) CountdownChip: live mm:ss, play/pause/reset (timerStartedAt), color ramp lumen>0.5>ember>0.2>coral, pulse+glow urgent<=0.2. DeadlineChip: Nd left/Due today/overdue, ember<=3 coral<=1, pulse when imminent/overdue. tsc+build clean. Caveats: no zoom yet (pan only); timer counts while mounted; bezier not orthogonal routing. NEXT: Phase 5 sharing; Phase 6 Canvas synergy; maybe zoom.
v77 WORKSPACE RE-ENGINEER (fixes drag-flyaway/connectors/error/layout): Root cause = Motion transform-drag inside pan-translated parent → coordinate compounding. FIX: manual pointer gestures. Workspace.tsx: pan state; single gesture ref (pan|drag|connect|resize) with surface pointer-capture; onPointerMove/endGesture dispatch by kind. Cards = plain absolute divs at screen=world+pan (NOT motion drag). beginDrag(origin=item.pos, dragSnap={id,x,y}=snap(origin+delta)), onDragEnd wsMove. beginConnect(id,side)→ptr world=client-left-pan, hover hit-test rectOf (world) → wsConnect. beginResize pointermove. rectOf(id) uses dragSnap during drag. Connector SVG inset-0 100% translated g translate(pan) — matches card space. sizes ref via ResizeObserver reportSize. Uniform corner grammar: TL move(GripVertical, beginDrag), TR resize(Maximize2), BL send(Send→menu wsToQ/wsToSignal), BR close(X→wsDelete). WsHeader = drag zone (onPointerDown→onMoveHandle unless [data-nodrag]); title/color/board badge data-nodrag. NoteBody toolbar = SINGLE ROW: B/I/highlight + Drop dropdowns (list/size/color/font) via openMenu state, Drop() helper. Add btn → floating pill bottom-center (left-1/2) clear of Signal panel + Recenter. tsc+build clean. Math verified. Caveats: no zoom; timer while mounted; bezier not orthogonal. NEXT: zoom (optional), Phase 5 sharing, Phase 6 Canvas synergy.
v78 WORKSPACE (toolbar fix / light mode / flowboard sections / context-aware +): (1) NoteBody FIX: savedRange ref, rememberSel() on keyup/mouseup/input captures Range if selection in editor, restoreSel() focuses+re-adds range, cmd() does restore→execCommand→remember→save. Removed hardcoded text color. Per-card light: types WorkspaceItem.light?; card frame +ws-light class + body bg-white; Sun/Moon toggle in WsHeader; index.css .ws-light overrides --color-vapor/mist/trace/glass-edge + note body/input colors. (2) FlowBoard sections: store wsBoards{name} state persisted q_wsboards; actions wsBoardName/wsBoardDelete(removes board+member cards+their conns)/wsBoardMove(dx,dy all members)/wsBoardOrganize(sort by incoming-connector count, grid perRow=ceil(sqrt), COLW300 ROWH240)/wsJoinBoard. Workspace.tsx: boardGroups (members by flowboardId, >=2 renders); FlowboardSection component (dashed rect auto-fit bbox+PAD28+TITLE30, draggable header, rename inline, organize LayoutGrid, close X→confirm). confirm modal state {kind:card|board,id,name,count}; card close handle → onRequestClose→confirm (uniform). (3) ContextualAdd mode-aware: onHome && homeView workspace→note/list/todo (wsAdd); board→setVibe(true) Canvas + sharedTools; signal→persona quick actions. Guarded existing lens/region cats with !(onHome&&workspace). tsc+build clean. Organize verified. Caveats: kept in-canvas pill + global + both; no flowboard scale handle; light per-card. NEXT: Phase 5 sharing; Phase 6 Canvas synergy; zoom.
v79 WORKSPACE POLISH (9 fixes): (1) removed in-canvas Add pill (Workspace) — universal + only; Recenter util kept; wsAdd staggers by count%5*28. (2) NoteBody toolbar FIXED: selectionchange global listener→savedRange, styleWithCSS true, runCmd restores range before execCommand; Drop arrows moved right (label+ChevronDown), menus right-0. (3) FlowboardSection 4 corner handles: TL drag / TR organize / BL close(confirm) / BR scale; removed header buttons; title inline TL + rename. (4) wsBoards pad field + wsBoardPad action; scale handle grows pad. Organize REWRITTEN in store (wsBoardOrganize): longest-path layering (roots indeg0→layer0, BFS max), columns by layer (x += maxW+90), rows by card size (y += h+40) — hierarchical, no overlap, arrow-direction aware. (5) index.css .ws-light .ws-head bg + .ws-head-title dark; header got ws-head/ws-head-title classes. (6) timer preset stepper TIMER_STEPS[1..240min], stepTimer(dir), fmtTimer (1h15 style); replaced number input with ChevronUp/Down stepper + clear; date clear button. (7) CountdownChip bigger (w-full, 26px mono), color ramp, pulse+glow urgent, WebAudio double-beep every 4s urgent / at done (respects w.reduced). (8) Continuum BoardGrid card: X → corner close handle (-right-2.5 -top-2.5 glass-elevated, group/bc hover). (9) confirm modal fixed inset-0 z-200 (was absolute). tsc+build clean; stepper+organize verified. Caveats: scale grows pad not cards; audio needs prior interaction; board masonry keeps reorder-drag. NEXT: Phase 5 sharing; Phase 6 Canvas synergy; zoom.
v80 (toolbar/light/anchor-menu/timer/signal-pins/board-pill): (1) NoteBody FIXED — body overflow-auto was clipping dropdowns. Toolbar shrink-0 outside scroll; menus position:fixed via getBoundingClientRect (menu state {id,x,y}); runCmd selects-all if no saved range; editable is flex-1 overflow-auto. (2) index.css .ws-light .ws-head-title white; WsHeader light bg=color-mix(color 82%,#000 4%), icon white when light. (3) anchor click: connect gesture +t0/sx/sy/moved; move sets moved if travel>6; endGesture: quick(<300ms)&&!moved&&!hoverTarget→setAnchorMenu({id,side,x,y}); store wsAddConnected(fromId,side,kind) places card GAP=90 off side, opposite-side connector, joins/creates fb. anchorMenu render (backdrop z-150 + menu z-160). (4) CountdownChip redesigned: radial ring R=22 C=2πR strokeDashoffset, centered time, label, Start/Pause/Reset pills, color ramp, pulse+glow+beep urgent. (5) Continuum WsSignalPins: 6 clockwise slots from TL (CW240 CH116 M20, corners+mid-L/R); SignalPinCard (corner handles TL open/BR close) + SignalPinPreview (note/list text OR todo progress + live mini timer, ticks 500ms). (6) Board: removed BoardChrome add pill; BOARD_PALETTE→src/boardPalette.ts (shared, avoid cycle); ContextualAdd board branch lists palette+Canvas; BoardGrid px-3 pt-3 to stop handle clip; BoardChrome cleaned unused vars. tsc+build clean; logic verified. DEFERRED: Board full handles (drag/scale/share) + snap-grid = convert masonry→free snap-canvas like Workspace (task 7, next focused piece). Caveats: timer sound needs prior interaction; pins cap 6.
v81 (toolbar root-cause / signal buffers / board menu+handles / flowboard close): (1) NoteBody: card body overflow-visible for kind==='note' (was overflow-auto clipping dropdowns); dropdowns inline-absolute under each DropBtn (data-note-menu/data-note-dropbtn, window pointerdown close guards those); card onPointerDown skips if closest('[data-nodrag]'); runCmd selects-all fallback + selectionchange range. (2) Continuum WsSignalPins: LEFT_BUF/RIGHT_BUF=72, TOPPAD+40, BOT_RESERVED=96; leftX/rightX/topY/midY/botY slots clockwise from TL. (3) ContextualAdd: modeMenu flag (onHome && homeView in signal|board|workspace) makes home menus EXCLUSIVE (general lens cats wrapped in if(!modeMenu)); board branch mirrors old pill (BOARD_PALETTE toggle + employee sharedTools OR manager/hr Canvas); employee board no Canvas; imports ME_ID from data, Share2. (4) Continuum BoardCard component: useDragControls (dragListener=false, TL GripVertical starts drag), TR Maximize2 scale(+1), BL Share2 publishTool (tools+manager/hr) else Minimize2 scale(-1), BR X close; scale adjusts spans[id]±6. imports Maximize2/Minimize2/GripVertical/useDragControls. (5) FlowboardSection: BL=scale, BR=close (swapped). tsc+build clean. DEFERRED task7: board free snap-canvas (masonry→canvas rewrite); board scale=rowspan not 2D. NEXT: task7 board snap-canvas; Phase 5 sharing; Phase 6 Canvas synergy. VERIFY IN BROWSER: note toolbar buttons+dropdowns.
v82 (Board→snap-canvas + audit): TASK 7 DONE. store: nowBoard items +pos?/size?; boardAdd staggers pos (n%4*300, n/4*240); +boardMove/boardResize actions+ctx. Continuum BoardGrid REWRITTEN as free snap-canvas (mirrors Workspace): pan state, gesture ref (pan|drag|resize), surface pointer-capture, GRID=24 snap, dragSnap guides, posOf(it,idx) fallback for un-positioned items, defaultSize (dash 480 else 300 ×260). BoardCard free-positioned (screen=world+pan), 4-corner handles TL drag(beginDrag)/TR resize(beginResize)/BL share(publishTool, tools+mgr/hr)/BR close; active card z=60 else 5. Recenter btn. ModeSwitch RESTORED (was in replaced range). AUDIT: removed dead grid code (spans/ResizeObserver/dragSnapToOrigin/cardRefs); removed unused imports (Workspace: ArrowLeftRight/Hand/MoreHorizontal; Continuum: Plus/Milestone/Minimize2); note frame overflow-visible + rounded-b body (dropdown clip fix); verified migration/coords via tsx. boardReorder now dead (store-only, left intentionally). tsc+build clean. NEXT: Phase 5 sharing (item.sharedWith→recipient ws); Phase 6 Canvas synergy; VERIFY note toolbar in browser.
v83 (leave policy / money mask / drag-to-Q / cross-lens docs / board combine): (1) Calendar leave policy: state reason/medDoc/rhPick, REASON_MAX=240; casualConverts=casual&&days>2→effectiveKind='earned'+notice; sickNeedsDoc=sick&&days>2→upload gate; isRestricted→holiday picker (restrictedHolidays=holidays.filter kind==='restricted'), hides duration/dates; reasonRequired=!restricted; canSubmit gates all; submit routes rh vs normal, passes reason+convertedFrom. store applyLeave +reason?/convertedFrom? (reducer records reason, conversion toast; rh skips balance). (2) Masked.tsx now reads w.showPay || local; pay token + PlainMode payslip use Masked; OrbitNode.value→ReactNode. (Money.tsx created then removed — consolidated on Masked.) (3) Workspace: import hit from dropzone; beginDrag sets w.setDragging(true); endGesture drag→hit(q)→wsToQ else move; always setDragging(false). (4) store answerRaw early block: visibleDocs by lens (employee: org|team-with-managerId|person-me), asksAboutDoc regex, titleHit by title words→answer in-lens BEFORE coverage/manager matchers (fixes cross-lens refusal). (5) store boardToQ(id)/boardCombine(idA,idB) actions+ctx (push QMsg to qLog, qOpen). Continuum BoardGrid endGesture: hit(q)→boardToQ, hit(token,excl bc-id)→boardCombine; beginDrag setDragging(true). BoardCard registers zone `bc-${id}` kind token data{boardId} via useEffect+interval; ref on root. import registerZone/clearZone/hit. tsc+build clean; logic verified (doc match, combine self-exclude). Caveats: Q responses deterministic; aggregate money not masked. NEXT: Phase 5 sharing; Phase 6 Canvas synergy; browser-verify note toolbar.
v84 (drag-to-Q fix / leave copy / mask default / token click / Phase 5 share): (1) TheCue: cue zone re-registers every 200ms while dragging (was one-shot on flag). Workspace endGesture: hit(q) BEFORE setDragging(false); board already correct order. (2) Calendar 3 sub-msgs one-line. (3+4) Continuum HomeOrbit TokenCard: removed onActivate={n.run} so face-tap = expand (onTap→setPinned), only expand's "Open {label}" btn runs n.run (has stopPropagation); this frees the Masked eye. showPay still defaults false. (5) store wsShare(id,personId)/wsUnshare reducer+action+ctx: sets sharedWith(dedup)+sharedBy=ME_ID, activity+toast; unshare clears when empty. Workspace card menu: shareOpen state, "Share with…" → people picker (excl ME_ID, toggle share/unshare, Check when on); header badge "shared N" when sharedWith.length. imports Share2 + ME_ID from data. tsc+build+dev-smoke clean; logic verified (doc access, combine self-excl, share dedup). Caveats: single-user POC (share shown via badge/activity/toast, no recipient view); Q deterministic. NEXT: Phase 6 Canvas synergy (item→Canvas tool, Canvas→workspace ref); verify note toolbar in browser; optional recipient "shared with me" surfacing.
v85 (Phase 6 Canvas synergy + robust drag-to-Q): PHASE 6 DONE. types WsKind+'tool', WorkspaceItem+toolRef. store wsAddTool(toolRef)→creates kind:'tool' ws card (dedup, homeView='workspace'), wsToCanvas(id)→seeds vibeApp checklist from list/todo items, vibeOpen. Workspace KIND_META/DEFAULT_SIZE +tool; ToolBody renders <VibeApp> read-only + "Edit in Canvas"; import VibeApp+Wand2. VibeStudio Send-to menu +Workspace dst (wsAddTool, PenSquare icon). Workspace card menu +"Turn into Canvas tool" (list/todo only, wsToCanvas). DRAG-TO-Q FIX: TheCue bar id="q-cue-bar"; dropzone.ts +hitQBar(x,y,margin=28) tests registered 'cue' zone OR live getElementById('q-cue-bar') rect w/ margin. Workspace endGesture + board endGesture use hitQBar (was hit(q) which depended on fragile registration timing). imports hitQBar both files. tsc+build+dev-smoke clean; logic verified (seed/dedup, hitQBar margin). NEXT: browser-confirm drag-to-Q + note toolbar; optional recipient "shared with me" view; Phase 6 polish (drag Canvas tile→workspace).
v86 (mask-by-default / drag z-order / top-clip): (1) store showPay:false always (removed localStorage read + write in togglePay) → sessions start masked. Masked.tsx wrapper +data-handle +onPointerDown stopProp (so Motion onTap on orbit token skips eye). Viz.tsx personal net headline wrapped in <Masked> (+import). Org aggregates left w/ global toggle. (2) Continuum stage motion.div className += dragging?'z-[60]':'' — lifts in-flight card above TheCue(z-50)/+/ModeSwitch(z-40); drop still via hitQBar coords. (3) Workspace surface + board surface (Continuum:512) overflow-hidden→overflow-visible: cards no longer clipped at top edge; dot-grid child inset-0 still bounded. tsc+build+dev-smoke clean. NEXT: browser-confirm drag-to-Q lands + card floats over Q + no top clip; note toolbar; optional recipient shared-with-me view. Caveat: overflow-visible lets cards spill over chrome mid-drag (ok for infinite canvas).
v87 (frictionless drag-to-Q / universal / Nows drag / light-mode / portal menus): (1) dropzone.ts qProximity(x,y,influence=150,release=46)→'none'|'influence'|'release'; hitQBar=prox!=='none'; qBarCenter(); qBarRect() helper. store qHover state+setQHover+reset on dragging off. TheCue: drop-ready(subtle)/drop-release(prominent) by qHover; dynamic hint. index.css .drop-ready subtle dashed, .drop-release solid+glow+scale. Workspace+Continuum board drag-move call setQHover(qProximity); endGesture hitQBar. (2) Tokens.tsx TokenFrame onDrag uses qProximity+setQHover, onDragEnd hitQBar+qBarCenter (import qProximity/hitQBar/qBarCenter). (3) Continuum WsSignalPins: window-listener drag (drag state, snap 24, subtle grid while dragging), boardMove persists pin pos, hitQBar→wsToQ; SignalPinCard +onGrab/dragging, 4-corner handles TL grip/TR open/BR close; orbit/panels/Focus untouched. (4) index.css .ws-light overrides for [data-note-menu] (+portal same-element selector), .ws-note-toolbar, .ws-timer/-track/-label/-soft, .ws-board-badge; Workspace tagged toolbar+timer+badge classes. (5) NoteBody dropdown → createPortal(document.body) fixed coords from btn rect, menuItems() helper, carries ws-light class; removed dup openMenu; import createPortal from react-dom. tsc+build+dev-smoke clean; proximity/portal logic verified. NEXT: browser-confirm all; optional live portal reposition; recipient shared-with-me view.
v88 (Q pull-in / panel-float persist / highlight toggle / dropdown polish / corner): (1) dropzone qProximity influence 150→20, release 46→6. Workspace+board endGesture Q-drop: setQPull ghost {title,x,y,tx,ty} via qBarCenter, setTimeout 380ms→wsToQ/boardToQ + clear; render motion.div ghost via createPortal(body) flying to Q. (2) TASK2: Tokens.tsx TokenFrame return → `node` const, `return isFloating ? createPortal(node, body) : node` (import createPortal). 4 panels (EmployeeToday/ManagerToday/HrToday/TheNow): removed early `if(!panel) return capsule`; now `collapsed` const + `capsule` const, main return `<>{collapsed&&capsule}<div ...visibility:collapsed?hidden:visible, pointerEvents none, aria-hidden>...</></>` — stays MOUNTED so portaled floats keep React owner alive. Close handle: if isFloating→dockCard+reset x/y else hide. (3) NoteBody: active state {bold,italic,highlight} via queryCommandState + bg-color probe on selectionchange refreshActive(); Btn takes `on` prop (lumen when active); toggleHighlight (hiliteColor transparent if active else #FFE08A). (4) portal menu width 168/max220 (was minWidth150). (5) WsHeader rounded-t-[13px]. tsc+build+dev-smoke clean. NEXT: browser-confirm float persistence + pull-in + highlight toggle; optional recipient shared-with-me. Caveat: hidden panels still register panel zone (ok).
v89 (rect-based drag-to-Q / module screens / panel-float dock / leave cleanup): (1) dropzone.ts: qProximityRect(cardRect,margin=24)/hitQBarRect test DRAGGED CARD RECT vs qBarRect inflated by margin (rectsOverlap helper); release=overlap(0), influence=overlap(margin). Kept point-based qProximity/hitQBar for fallback. Workspace drag move+end compute card rect = surfaceRect + snappos + pan + size(DEFAULT_SIZE), call qProximityRect/hitQBarRect. Board same via defaultSize. Signal pins rect from nx/ny+CW/CH. (2) Tokens.tsx onDrag/onDragEnd use ref.getBoundingClientRect()→qProximityRect/hitQBarRect (fixes module screens Payroll/Documents). imports qProximityRect/hitQBarRect in all 3 files. (3) TASK3 FIX: 4 panels registerZone('panel-*', PANEL_OPEN ? panelRef.current : null) — collapsed panel no longer registers panel zone, so floated card reposition/Q-drop near hidden panel no longer docks/vanishes. (4) Calendar removed redundant top "prominent balance" box (kept per-type cards). tsc+build+dev-smoke clean; rect proximity verified (bottom edge within 24px→influence). NEXT: browser-confirm edge-trigger + module drag + float persistence; optional recipient shared-with-me.
v90 (drag z-order / Canvas-free MyWorld / toast placement / orbit smoothness): (1) Tokens.tsx zIndex: drg?9997 (was floating?30) → above Q(z50); stage already z-60 on w.dragging. (2) Toasts.tsx top-[76px] center (was top-6, overlapped menu); restyled accent bar+icon chip+glow, z-200. (3) Tokens onDrag: setQHover only on change (lastQHover ref), setPreview only on change (functional guard) → kills per-frame re-render jank; reset lastQHover on dragEnd. (4) CANVAS GATED for employee: Workspace 'Turn into Canvas tool' (manager/hr only), Continuum board empty 'Build in Canvas' btn (mgr/hr), board hint copy lens-aware, GestureLegend filter Canvas for employee. Action dock + Tour already gated. (5) Signal pins grid+snap already present (v87). (6) rect drag-to-Q (v89) covers modules. tsc+build+dev-smoke clean. NEXT: browser-verify z-order-over-Q + module drag + orbit smoothness + toast; #3 dragged-card width restore needs concrete repro if still broken.
v91 (drag friction / plain-mode regression / UI+a11y audit): (1) index.css .is-dragging{transition:none!important} + .is-dragging.node-lift:hover{transform:none}; Tokens adds `is-dragging` class when drg — ROOT CAUSE of drag friction was .node-lift transition:transform .2s animating every Motion frame. (2) Continuum: moved `if(w.plain) return <PlainMode/>` to AFTER lineage useMemo (was before → fewer-hooks crash). (3) Calendar: LEAVE_META{earned:Plane/lumen,sick:Thermometer/coral,casual:Coffee/ember,rh:Star/halo}; type grid gap-3 py-3 distinct icons; steps wrapper minHeight 486 (constant height); RH picker: rhTotal/rhLeft quota badge + disable when 0, rhQuery+rhFiltered search when >6, fixed height 176 scroll, selected=border accent+Check; footer regrouped (border-t divider, w-full h-12 submit + h-10 secondary). (4) Calendar left col → flex-col: calendar own scroll region + context card shrink-0 maxHeight 232 w/ inner scroll; ctxTab 'month'|'year'; myLeaves=leaves.filter(personId===ME_ID) rendered w/ status colour. (5) BoardCard BL: share (tools+mgr/hr) else boardToQ Sparkles — 4 corners always; SignalPinCard +BL onAskQ. (6) A11Y: 696 text-* bumped one notch (9/10→11,11→12,12/xs→13, none <11px); --color-trace lifted both modes; 568 p/px/py/gap/m utilities snapped to 4px grid (1.5→2,2.5→3,3.5→4) EXCLUDING positional/-left-2.5/w-/h-/text- (36 handle offsets verified intact). tsc+build+dev-smoke clean. NEXT: visual re-check of dense spots; RH quota should reconcile vs taken RH.
v92 (calendar layout fix / payroll self-service / assets): (1) REGRESSION FIX: Calendar left column + context card need min-w-0 (grid child min-width:auto let the wide ribbon blow out the 1fr track and push the 360px composer off-screen). (2) Composer shell: glass-elevated h-full flex flex-col (dropped lg:sticky/maxHeight/minHeight486); steps = flex-1 min-h-0 overflow-y-auto; footer moved OUTSIDE steps div w/ shrink-0 (always visible) — removed one stale </div> at composer tail to rebalance. (3) types.ts +Reimbursement +AssetItem; data.ts +REIMBURSEMENTS +ASSETS (deduped type imports across the multiple `import type` lines). store: +reimbursements/assets state, claimReimb action (activity+toast), imports REIMBURSEMENTS/ASSETS + Reimbursement/AssetItem types. (4) NEW src/components/PayrollExtras.tsx → PayslipLibrary (Blob download of slip) + Reimbursements (claim form: title/category/amount/receipt required); wired into employee payroll RegionWrap as 2-col grid. (5) NEW src/components/AssetsSection.tsx (icon per category, Blob download of register); wired at end of Documents. (6) store answerRaw: early matchers for reimburse|claim|expense|receipt → payroll; asset|laptop|badge|monitor → documents; payslip|salary slip|download slip → payroll. Verified routing via tsx. tsc+build+dev-smoke clean. NEXT: manager-side reimbursement approval queue; real PDF slips.
v93 (stacked month ribbons + Assets category): (1) Calendar ribbon view: replaced single horizontal scroll SVG with MONTHS.map(<MonthRibbon>). New MonthRibbon component: per-month responsive SVG (viewBox 0 0 1000 76, width 100%, preserveAspectRatio none, vectorEffect non-scaling-stroke on all strokes/rings), xAt=frac of month, sine wave, same event dots/attendance rings/today pulse/selection/day labels; tap→pick, hover→setHover. Old W/xAt/yAt/scroll handlers/isInteracting now dead but harmless (build tolerates). (2) Documents: cats +'Assets'; showAssets=(All|Assets), showDocs=(!Assets); Assets tab has count badge; shared+docs grid gated behind showDocs (grid brace closed with }); <AssetsSection topLevel={cat==='Assets'}/> gated on showAssets; header→'Documents, policies & assets'; +assets from store destructure. AssetsSection +topLevel prop (no mt-6 when true). tsc+build+dev-smoke clean. NEXT: browser-check ribbon at narrow widths; asset return/check-in workflow; manager reimbursement approval queue (still pending from v92).
